# Per-Request Stage & History Visibility — Progress Ledger

Plan: docs/superpowers/plans/2026-07-14-per-request-stage-visibility.md
Branch: feat/per-request-stage-visibility

## Tasks
| Task | Description | Status | Commit |
| --- | --- | --- | --- |
| 1 | StageHistoryVisibility enum + StageHistoryVisibilityResolver service | complete (review clean, no findings) | 38a60b07 |
| 2 | Redact EngineRequestController::history() using the visibility resolver | complete (review clean after fixture redesign, independently re-verified no collision) | 16dd2c36 |
| 3 | Filter stage nodes/edges in EngineRequestController::graph() | complete (review clean, fix independently verified correct) | e1493cd3 |
| 4 | Frontend type + timeline redaction display | complete (review clean, shell-incident forensics independently confirmed no leftover state) | 55d94164 |
| 5 | Confirm process rail degrades gracefully with filtered graph data | complete (review clean, zero implementation change as predicted) | 3c4b30e5 |

## Session notes
- Task 1: implementer (haiku) transcribed the brief's enum/resolver code correctly, and correctly deviated from the brief's literal test snippet where the snippet itself would have failed against the real schema — `stage_permissions.display_label` and `engine_requests.reference`/`created_by` are NOT NULL columns with no defaults, so the implementer added those fields to the test fixture. Reviewer independently checked the migrations and confirmed these were required fixes, not scope creep. Review approved clean, no findings.
- Task 2: implementer (sonnet) correctly identified a real defect in the plan's own brief — 2 of the 4 prescribed test fixtures constructed users who failed the pre-existing, out-of-scope `EngineRequestPolicy::view()` gate (current-stage-only VIEW check) before ever reaching the new redaction logic, producing 403s instead of the predicted assertion failures. Reported BLOCKED with root cause + 3 options rather than guessing — correct call, since fixing it required a fixture redesign the brief didn't specify. Controller (not a subagent) redesigned both fixtures directly: a user-scoped StagePermission grant on the CURRENT stage (satisfies the pre-existing gate) combined with deliberately absent access on the EARLIER stage under test. Hit and resolved a second-order trap along the way: the resolver keys off `entry->toStage ?? entry->fromStage`, so granting the top-level-gate-satisfying stage access on the SAME stage an entry resolves against silently also satisfies that entry's redaction check — first attempt at fixing the second fixture accidentally made the "sanitized" entry resolve FULL this way. Fixed by targeting a different entry (SUBMIT, to_stage REVIEW) than the stage used to satisfy the view gate (COMPLETED). Full `EngineRequestTest.php` suite (41 tests) green after the controller change, no regressions. Flagged for Task 3: `graph()` shares the identical `authorize('view', ...)` gate — same fixture trap is plausible there. Review independently re-verified the "no collision" claim from scratch (traced resolver code, transition-recording code, and fixture StagePermission rows directly) rather than trusting the report — approved clean, no findings.
- Task 3: implementer (sonnet) implemented the controller filtering and all 3 new tests correctly (verified via its own pre-flight direct read of the live fixture, exactly as instructed after Task 2's precedent), but found a genuine defect in the brief itself: a PRE-EXISTING test (`test_graph_marks_executed_current_possible`, predates this whole feature) asserts the COMPLETED node's `state` for `executor`, who has zero StagePermission access to COMPLETED — with correct filtering applied, that node is now legitimately absent and the assertion errors on null. The brief's Step 4 claimed this test was safe because "COMPLETED is asserted only via execute_stage_ids assertNotContains" — but that description actually belongs to a different, sibling test; the implementer verified this directly rather than trusting the brief's claim, correctly declined to edit an existing test outside its authorized scope, and escalated BLOCKED with two resolution options. Controller fixed it directly: switched that one test's acting user to a fresh SYSTEM_ADMIN (bypasses filtering entirely), preserving the test's original state-annotation intent without coupling it to the new permission fixture. Full suite green after the fix and after Pint (0 failed, 45 deprecated-only, 212 assertions). Review independently confirmed the fix preserved all 3 original assertions byte-identical and no other pre-existing test was silently touched — approved clean.
- Task 4: implementer (sonnet) delivered the frontend type/composable/component change exactly per brief, plus one incidental fixture fix in `useEngineStagePath.test.ts` (new required `EngineHistoryEntry` fields needed adding to an existing test literal to keep it typechecking — correctly in scope, not overreach). Subagent self-disclosed a mid-task shell mishap (a `cd`+`git stash pop` briefly ran in the wrong directory, caught and resolved with no data loss) and that this task's own report-file slot held a stale unrelated report before being overwritten — both handled transparently. Given a safety-classifier outage flagged this run for extra scrutiny, the controller independently pre-verified before dispatching the task reviewer: re-ran both Vitest files (13/13 pass), re-ran ESLint (clean), re-ran `pnpm typecheck` (zero new errors), and read the actual commit diff directly against the plan's given code (byte-matching, only cosmetic Prettier reformatting beyond specified changes) — this was extra diligence on top of, not instead of, the normal task-review gate.

- Task 5: implementer (haiku) delivered exactly as predicted — verification-only, zero implementation changes to `useEngineStagePath.ts`, 2 new regression tests (filtered-graph node count, null-to_stage no-throw) both correctly fixtured with Task 4's new required `restricted`/`restricted_label` fields. Review independently re-ran tests/lint/format, confirmed no premature implementation edits, approved clean.

- Final whole-branch review (opus): Ready to merge, no Critical/Important findings. Confirmed cross-task consistency (VIEW-threshold + SYSTEM_ADMIN bypass identical across resolver/history/graph), zero scope creep (Designer endpoints, audit_logs, report exports all untouched), frontend/backend wire contract matches exactly, and both live-fixed test fixtures (Tasks 2/3) converged on a coherent, non-contradictory final state. One Minor doc-hygiene recommendation (update design doc status/§7) — applied directly, commit 168a29a9.

## Plan status: COMPLETE (all 5 tasks done, reviewed clean, final whole-branch review approved, design doc updated) — ready for superpowers:finishing-a-development-branch
